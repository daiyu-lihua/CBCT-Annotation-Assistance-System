# 改动说明

本文档记录 ToothSeg 模型接入 CBCT-Annotation-Assistance-System 项目的全部改动，以及推理算法在接入过程中完成的优化。改动基准为项目交接时的插件骨架与 mock 服务端，目标是在不破坏 README 规范目录结构和统一接口协议 v1 的前提下，把 mock 推理替换为真实的 ToothSeg 双分支推理。

## 改动总览

| 位置 | 改动类型 | 内容 |
| --- | --- | --- |
| implementation/model/toothseg | 新增 | ToothSeg 算法代码自包含集成（推理入口、MemSafe 推理器、迷你 toothseg 包、后处理脚本） |
| implementation/model/weights | 新增 | 语义分支与实例分支两组 nnU-Net 权重（含 plans、dataset.json、fold_5 checkpoint） |
| implementation/server/inference/toothseg_server.py | 新增 | 真实推理服务端，实现 9 个统一接口，/predict 调用 ToothSeg |
| user/plugin/lib/ApiClient.py | 新增 | 插件侧 HTTP 客户端，供前端调用 9 个接口 |
| user/plugin/CBCTAnnotator.py | 修改 | 由纯骨架扩展为可连接服务端、导入影像、发起预测的可用插件 |
| user/launcher | 新增 | 一键启动脚本与环境说明，降低使用门槛 |
| README.md | 修改 | 补充模型接入位置、启动方式与验证说明 |

## 新增与修改的文件

### 模型与算法层

模型代码按 README 规范落在 `implementation/model/toothseg/`，权重落在 `implementation/model/weights/`。

- `run_toothseg.py`：端到端推理入口，改编自 ToothSegWork 交接版。与原版的差异包括：路径按脚本位置自动定位并支持 `TOOTHSEG_HOME`、`TOOTHSEG_NNUNET_RESULTS` 环境变量覆盖，不再硬编码；后处理子进程注入 PYTHONPATH 指向本目录内的迷你 toothseg 包，与 ToothSegWork 及 site-packages 完全解耦；nnUNet_raw、nnUNet_preprocessed 中间目录默认放在 `implementation/model/work/`。
- `memsafe_inference.py`：内存安全推理器 MemSafePredictor，继承 nnUNetPredictor 并重写滑窗与导出链路，是本次算法优化的核心（详见下文算法优化一节）。
- `toothseg/`：迷你 toothseg 包，仅保留后处理需要的 `copy_geometry` 等工具函数，保证整个模型层自包含。
- `postprocess_predictions/`：后处理链路脚本，依次执行 border-core 转实例、重采样回原始 spacing、mincost 牙位编号自纠正，另含 majority 牙位分配备选方案与 sitk 兼容层。
- 权重两组：`Dataset121_ToothFairy2_Teeth`（语义分支，33 类牙齿区域，0.256m 配置）与 `Dataset123_ToothFairy2fixed_teeth_spacing02_brd3px`（实例分支，border-core 语义，0.2mm 配置），均取 fold_5 的 `checkpoint_final.pth`。
- 2026-09-02 起为推送 GitHub，权重目录与历史输出产物临时移至仓库同级（`../model_weights/` 与 `../CBCT_data/`，单文件约 236 MB 超过 GitHub 100 MB 限制），运行前放回 `implementation/model/weights/` 即可，代码无需改动，详见 `模型权重放置说明.md`。

### 服务端

`implementation/server/inference/toothseg_server.py` 实现 9 个统一接口（协议 v1），Base URL 为 `http://127.0.0.1:8000/api/v1`：

| 接口 | 方法 | 作用 |
| --- | --- | --- |
| /status | GET | 服务与 GPU 状态探测 |
| /config | GET | 返回标签模板、模式、路径等配置 |
| /cases | POST | 病例目录登记 |
| /images/inspect | POST | 影像元信息读取 |
| /agent/recommend_mode | POST | 根据影像规模推荐推理模式 |
| /predict | POST | 发起 ToothSeg 推理，阻塞至完成 |
| /check_label | POST | 标注结果校验 |
| /export | POST | 标注导出 |
| /agent/log | POST | 前端行为日志落盘 |

服务端保留 `mock_server.py` 作为对照实现，两者共用 `assets/label_spec_96.txt` 的 96 类稠密标签规范（模板 ID `teeth-dense-96`），前端切换真实服务端时无需改动标签模板逻辑。推理产物写到 `data/outputs/toothseg/jobs/<pred_id>/output/final_prediction/*.nii.gz`，`mask_path` 直接返回给插件加载。

### Slicer 插件前端

`user/plugin/CBCTAnnotator.py` 在交接版骨架基础上完成服务连接层与数据导入：服务地址输入与连接测试（/status）、配置加载（/config）、CBCT 影像图形化导入与显示、信息卡展示。`user/plugin/lib/ApiClient.py` 为独立 HTTP 客户端，放在 lib 子目录以避免被 Slicer 误当作脚本模块扫描；`predict()` 读超时设为无限并在子线程调用，长推理期间界面不卡死。

### 启动脚本与文档

`user/launcher/` 提供一键启动 ToothSeg 服务的 bat 脚本（指向 nninteractive 环境的 python.exe）与环境说明。README.md 补充了模型接入位置与启动方式。Slicer 插件加载采用启动参数方式：`Slicer.exe --additional-module-paths <插件目录>`，这是经过验证的唯一可靠注入方式（详见启动验证一节）。

## 算法优化

### MemSafePredictor 内存安全推理

原版 nnUNetPredictor 在 8GB 显存卡上跑完整 CBCT 会在导出阶段溢出。MemSafePredictor 把整条链路改成流式分块处理：

- 滑窗阶段按 fp16 logits 张量大小自动选择在显存或内存累积，避免某一步 OOM 导致整轮推理作废。
- 导出阶段由主进程流式完成，z 轴分块做重采样、argmax 与概率落盘，峰值内存约 5GB，实测可在 8GB 卡上稳定跑完。
- fp32 中间量按约 1.2e8 元素（约 0.5GB）的分块预算切分，RAM 常驻 logits 设独立预算。
- z 轴相邻块的重采样使用线性插值重叠窗衔接，`verify_interp_against_torch()` 用 torch 原生插值结果做一致性校验，保证分块插值与整图插值误差可忽略。
- 语义分支概率按 z 块以 fp16 落盘缓存（`_probs_dir_for`），供牙位分配阶段复用，`cleanup_probs_cache()` 统一清理。

### 滑窗步长参数修复

原版调用方式给 `pred.step_size` 赋值，但 nnUNetPredictor 父类的真实属性名是 `tile_step_size`，导致步长设置从未生效、推理始终以默认步长运行。本次在构造 MemSafePredictor 时直接传入 `tile_step_size`，并把三种推理模式映射为不同步长：fast 0.8、balanced 0.5、fine 0.4（步长越小越精细也越慢）。

### CUDA 显存分配调优

语义分支单个滑窗 tile 的前向峰值已经贴近 8GB 卡上限（allocated 6.01GiB，reserved-unallocated 仅 31MB），解码器 instance_norm 层再申请 1GB 时分配器因碎片化拼不出空闲块，触发 CUDA OOM，Windows 上早期表现为原生段错误 0xC0000005。修复方式是在 torch 启动 caching allocator 之前设置：

```
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,garbage_collection_threshold:0.5,max_split_size_mb:128
```

`expandable_segments` 大幅减少碎片（pytorch 报错信息点名建议），垃圾回收阈值与大块切分限制作为兜底。该结论经 faulthandler 与 pytorch 自身诊断确认。

### 双分支子进程隔离

full 模式下语义分支与实例分支拆成两个独立子进程依次运行，分支结束时操作系统完整回收显存，避免双分支在同一进程内叠加占用。服务端再包一层子进程调用 run_toothseg.py，且服务进程自身不 import torch、不初始化 CUDA，规避了 Windows 上父子进程 GPU 竞争导致的 0xC0000005。/status 中的 GPU 信息通过 nvidia-smi 子进程探测获取，服务进程长期驻留也几乎不占显存。

### 牙位分配先验路径修复

牙位编号阶段依赖 `fdi_pair_distrs.json` 的高斯分布先验。原脚本对该文件的默认值是相对当前工作目录的路径，只有在特定目录下启动才能解析正确。本次改为显式传绝对路径，任何目录启动均可用。

### 输入与资源保护

- ToothSeg 要求文件名带 `_0000` 后缀，服务端在 /predict 内自动把用户图像复制到任务目录并重命名，前端无需关心该约定。
- GPU 独占锁保证同一时刻只有一个 /predict 在跑，第二个请求立即返回 `PREDICT_IN_PROGRESS`，避免并发推理挤爆显存。
- ROI 仅记录不裁剪，全图推理保证牙位编号空间先验正确。
- 后处理子进程注入 PYTHONPATH 且优先级高于 site-packages，环境中即使另装了 toothseg 也不会引用错位。

## 启动与验证结果

1. 服务端：`user/launcher/一键启动ToothSeg服务.bat` 或直接用 nninteractive 环境的 python.exe 运行 `toothseg_server.py`，地址 `http://127.0.0.1:8000/api/v1`。/predict 自检用例完整跑通，耗时约 194 秒。
2. Slicer：使用 `--additional-module-paths` 启动参数指向 `user/plugin` 后，CBCTAnnotator 模块成功注册（模块总数 142，含本插件），界面正常显示中文 UI，服务地址预填 `http://127.0.0.1:8000/api/v1`。
3. 排查记录：QSettings 写入的 AdditionalPaths 在 Slicer 5.12.3 启动时不会被读取；运行时通过 Python 侧改 factoryManager 的 searchPaths 也不会触发脚本模块重扫（脚本模块注册只发生在启动早期），因此最终固定采用启动参数方案。
