# 改动说明

本文档用于记录当前项目主要修改内容。当前项目阶段以 3D Slicer 插件作为前端，以本地 FastAPI 服务作为后端，以 ToothSeg 语义分割作为当前主流程模型。

## 当前版本：带鱼修改内容

该版本基于拉取后的 `slicer-ui-reuse-label-update` 分支文件继续整理，重点解决 3D Slicer 前端流程冗余、本机中文路径读取失败、模型权重路径固定、推理进度不同步以及 RTX 4060 Laptop 8GB 显存下 ToothSeg 语义分割难以跑通的问题。当前目标是让项目进入更稳定的本地单卡运行形态。

### 前端流程整理

- 删除“使用当前病例”“读取信息”按钮，将病例绑定和影像检查改为随 3D Slicer 原生文件导入后自动检测执行。
- 删除“导入 Volume”“导入分割”按钮，前端不再重复实现 Slicer 已有导入能力。
- 删除“选择提示”模块和“推荐模式”入口，当前固定采用 balanced 处理方式，避免只有一种主流程时产生误导。
- 保留前端“降采样间距”可调能力，用户仍可在插件中设置 `spacing_mm`，用于控制推理前重采样分辨率。
- 新增场景自动检测逻辑：定时识别当前 Volume、Segmentation / LabelMap 是否可用，可用则自动绑定到当前病例流程，不可用则给出提示。

### 后端进度与中止机制

- 新增真实后端进度接口 `GET /api/v1/predict/progress/{case_id}`，前端进度条不再靠时间估算，而是读取后端当前阶段。
- ToothSeg 语义推理流程增加阶段回调，覆盖接收请求、环境检查、复用检测、复制输入、预处理、启动 nnU-Net、运行中日志、输出检查、标签映射、结果统计和完成状态。
- 前端新增“中止分割”按钮；后端接到取消请求后会终止正在运行的 nnU-Net 子进程，并根据用户是否保留复用包来检测或删除可复用文件。
- 前端进度显示补充运行时长，避免长时间推理时只看到静态状态。

### 本机路径与权重适配

- 权重路径从固定 `implementation/model/weights/` 改为自动发现 `nnUNet_results` 根目录，支持 `nnUNet_results`、`TOOTHSEG_NNUNET_RESULTS`、`CBCT_NNUNET_RESULTS`、项目内 `ToothSeg/nnUNet_results/`、本机 `D:\ToothSegWork\nnUNet_results/` 等路径。
- 原始影像、输入缓存、预处理文件、分割结果、复用包和服务端 jobs 默认统一放入英文 runtime：`D:\ToothSegWork\_runtime\`。
- 修复 SimpleITK 在 Windows / Conda 环境下读取中文路径失败的问题：即使用户从中文路径导入病例，后端也会先复制到英文缓存后再读取。
- 说明文档补充路径要求：长期使用建议原始 CBCT、权重目录、运行缓存和分割结果均使用英文路径，便于复现和排错。

### 低显存推理配置

- 新增项目级推理配置文件 `config/inference_profiles.json`，配置文件只管理推理策略，不修改 nnU-Net 原始 `plans.json` 或 checkpoint。
- 默认启用 `low_vram` profile，面向 RTX 4060 Laptop 8GB 显存优先跑通一次。
- `low_vram` 使用已有 `MemSafePredictor` 低显存子进程作为推理引擎，模型 forward 仍在 GPU 上执行，滑窗累积和导出阶段尽量降低 GPU 显存峰值。
- 低显存默认参数：`step_size=0.75`、关闭 TTA、`npp=1`、`nps=1`、不保存 probabilities、限制 OpenMP / MKL / OpenBLAS / NumExpr 线程数。
- 前端传入的 `spacing_mm` 优先级最高，profile 只控制除降采样间距以外的推理参数。

### 文档与项目状态

- 更新 `README.md`、`implementation/server/inference/README.md`、`模型权重放置说明.md`、`user/launcher/环境说明.md`，统一说明英文路径、权重自动发现和低显存推理配置。
- 当前主流程仍为 ToothSeg Dataset121 语义分割，仅输出牙位语义标签；`toothseg-full` 完整双分支仍作为未来高级模式占位。
- 当前分支目标是先保证 4060 8GB 单卡可以稳定完成一次语义分割，再逐步回调更高质量的降采样间距与推理参数。

## 当前版本：王璟梦修改内容

该版本主要解决了“推理完成后导出阶段崩溃”的严重故障，并通过 3D Slicer 端到端验证闭环。

### 修复 nnU-Net 导出阶段 WinError 87 崩溃

- 故障现象：点击“开始分割”后，滑窗推理 32/32 tile 全部完成（进度长期停在 55%），随后在导出阶段崩溃，任务以 `error` 结束，分割结果全部丢失。
- 根因：nnU-Net 在 Windows 上使用 spawn Pool 做分割结果导出，主进程需把整卷 logits 通过匿名管道传给导出 worker，大体积数组触发 `OSError: [WinError 87] 参数错误`，属 CPython multiprocessing 在 Windows 的已知问题。
- 修复方式：修改 `implementation/model/toothseg_semantic.py`，将 nnUNetv2_predict 命令参数从 `-npp 1 -nps 1` 改为 `-npp 0 -nps 0`，进入官方 sequential 模式——预处理、推理、导出全部在主进程内完成，预测数组零跨进程传输，从机制上根除该类崩溃，且不修改任何第三方库源码。
- 单图场景无性能代价：原先也只有 1 个导出 worker，顺序执行耗时基本相同，反而省去了大数组序列化（pickle 副本）的内存峰值。

### 附带修复

- `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 改为仅在非 Windows 系统设置：Windows 版 PyTorch 不支持该分配器配置，原无条件设置每次运行都产生 UserWarning 噪音。

### 端到端验证结果（2026-09-05 实测）

- 通过 3D Slicer 插件发起语义分割（balanced 模式、降采样 0.75mm），复用包机制自动复用上次失败的降采样输入，跳过预处理直接推理。
- 服务端日志确认实际命令行已带 `-npp 0 -nps 0`，导出阶段在主进程顺利完成，全程未再出现 WinError 87 / AssertionError。
- 推理成功返回，插件界面状态变为“AI 分割完成，结果已加载”；共分割出 26 颗牙（上颌 13 颗、下颌 13 颗，缺位为智齿/缺失牙，符合该病例牙列）。
- 附带改善：`_probe.nii.gz` 探测错误从上一轮 17 条降为 1 条，`expandable_segments` 警告消失。

### 新增文档

- `docs/故障排查/2026-09-05_nnUNet导出WinError87修复报告.md`：完整记录本次故障的现象、根因分析、修复方案、变更清单、验证测试与遗留事项。

### 使用说明补充（针对近期常见疑问）

- 插件显示“模型：未完整加载”不代表故障：该状态是连接瞬间服务端环境自检的快照（权重文件存在 + `nnUNetv2_predict` 可找到，两项任一不满足即显示），点击“刷新服务”即可更新。模型权重本就不常驻显存，只在推理子进程运行期间临时加载，跑完即释放。
- 选择 `toothseg-full` 后点击分割直接失败属预期行为：该模型是未来高级模式的接口占位，服务端会直接返回“暂未启用”，当前请使用 `toothseg-semantic-05mm`。
- 推理运行时硬盘持续有数据写入属正常现象：复用包机制会在 `D:\ToothSegWork\_runtime\reuse_packages\` 落盘输入副本、降采样输入、分割结果与运行日志，用于复用提速与排错。

### 当前需要注意的问题

- 服务端必须使用一键启动脚本（`user\launcher\一键启动服务.bat`）启动，否则 PATH 中找不到 `nnUNetv2_predict`，模型探测会失败。
- 0.75mm 全卷滑窗推理在本机约 45 分钟，首个 tile 因 GPU/CUDA 初始化常达 20 分钟以上，属环境特性；如需提速可评估更大的降采样间距。
- 本机中文用户名导致的 SimpleITK 中文路径写入问题仍依赖 `sitecustomize.py` 的 nibabel 兜底补丁，升级或重装环境后需重新验证。
- 每次修改后端代码后，需要重启后端服务；每次修改 Slicer 插件后，需要重新加载插件或重启 Slicer。
