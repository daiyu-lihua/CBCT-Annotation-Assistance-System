# 改动说明

本文档用于记录当前项目主要修改内容。当前项目阶段以 3D Slicer 插件作为前端，以本地 FastAPI 服务作为后端，以 ToothSeg 语义分割作为当前主流程模型。

## 上一个版本：王璟梦修改内容

该版本主要完成了项目从“基础框架”到“可联调系统”的搭建。

### 主要内容

- 搭建了项目基础目录结构，包括 `user/`、`implementation/`、`docs/`、`data/` 等目录。
- 初步建立了 3D Slicer 插件前端文件：`user/plugin/CBCTAnnotator.py`。
- 新增前端接口封装文件：`user/plugin/lib/ApiClient.py`。
- 搭建 FastAPI 后端服务，包括：
  - `implementation/server/inference/mock_server.py`
  - `implementation/server/inference/toothseg_server.py`
- 接入 ToothSeg 语义分割模型适配层：`implementation/model/toothseg_semantic.py`。
- 保留 ToothSeg 完整双分支相关代码，作为未来高级模式参考。
- 新增一键启动脚本：`user/launcher/一键启动服务.bat`。
- 编写了基础说明文档、接口说明和插件使用说明。

### 该版本的项目状态

- 前端可以连接本地服务。
- 后端可以返回服务状态、模型配置和标签模板。
- mock 服务可以用于前端流程联调。
- ToothSeg 语义分割服务已经具备基础调用能力。
- 完整实例级双分支流程暂未作为主流程启用。

## 当前版本：带鱼修改内容

该版本主要围绕“真实使用体验”和“本机低显存运行”进行优化，使插件更适合在 3D Slicer 中实际操作。

### 前端界面优化

- 将插件界面风格调整为更接近 3D Slicer / nnInteractive 的原生桌面 GUI 风格。
- 减少网页式卡片、宽按钮和过强的颜色装饰。
- 删除不必要的数据导入按钮，默认使用 Slicer 原生方式打开 CBCT 影像。
- 删除步骤编号式排版，改为按功能直接分区：
  - 服务与模型
  - 当前病例
  - AI 分割
  - 选择提示
  - 人工修正
  - 标签集合与标签列表
  - 质检与导出
  - 日志
- 日志区域移动到底部，方便查看流程记录。
- 分割按钮下方新增进度条，用于显示模型推理过程。

### 选择与人工修正工具

- 新增点选、框选 ROI、圈画等选择提示入口。
- 当前 ToothSeg 语义模型仍按整图推理，这些选择工具主要作为后续交互式模型或高级模式的输入预留。
- 人工修正区接入 Slicer 自带 Segment Editor 工具，包括：
  - 涂抹
  - 擦除
  - 图形圈画
  - 剪刀
  - 平滑
  - 连通域处理

### 标签集合逻辑

- 修改标签显示逻辑：插件打开时不再直接显示全部标签模板。
- 标签模板只作为命名规则；真正的标签列表必须来自模型分割结果、导入的分割文件或用户创建的标签集合。
- 新增标签集合下拉管理功能。
- 每个标签集合可以进行：
  - 选择
  - 重命名
  - 删除
- 标签列表显示内容包括：
  - 是否显示
  - 标签名称
  - 标签颜色
  - 审核状态

### 可复用规则

- 新增模型推理复用包机制。
- 每次模型推理前，会根据原始图像内容计算 SHA256 哈希值，用于判断这份图像是否已有可复用结果。
- 每份图像对应一个复用包目录，内部记录：
  - 原始图像信息
  - 图像哈希值
  - 降采样后的输入文件
  - 推理结果文件
  - 本次运行状态
  - 可读摘要
- 如果推理中断但已经完成降采样，下次可以复用降采样文件，减少重复等待。
- 前端新增：
  - 保留复用包选项
  - 检测复用包按钮
  - 删除复用包按钮
- 删除复用包时按整个复用包文件夹删除，避免留下不完整目录。

### 可调降采样参数

- 前端 AI 分割区域新增“降采样间距”输入框。
- 参数单位为 mm。
- 当前范围：`0.50 mm - 2.00 mm`。
- 默认值：`0.75 mm`。
- 数值越大，图像体素数量越少，显存压力越低，但分割细节会下降。
- 数值越小，细节越好，但显存压力越高。
- 该参数会传给后端，并实际影响 ToothSeg 语义分割前的图像重采样。
- 建议本机 RTX 4060 Laptop GPU 先使用 `1.00 mm` 或 `0.75 mm` 跑通流程，再尝试 `0.50 mm`。

### 权重路径说明

- 不再单独维护模型权重放置说明文档。
- 权重路径读取逻辑已经统一写入 `README.md`。
- 推荐团队统一把权重放在：

```text
D:\ToothSegWork\nnUNet_results
```

- 权重文件不上传 GitHub。
- GitHub 只保存源码、配置和说明文档。

### 当前需要注意的问题

- 当前主流程仍是 ToothSeg 语义分割，不是完整实例级双分支。
- 8GB 显存运行 `0.50 mm` 仍可能出现显存不足，建议优先使用 `0.75 mm` 或 `1.00 mm`。
- 如果后端端口 `8000` 被占用，需要关闭旧服务或改用其他端口。
- 每次修改后端代码后，需要重启后端服务。
- 每次修改 Slicer 插件后，需要重新加载插件或重启 Slicer。
