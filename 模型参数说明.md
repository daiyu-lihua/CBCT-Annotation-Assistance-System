# 模型参数说明

## 1. 当前项目使用的模型

| 项目 | 内容 |
| --- | --- |
| 模型来源 | ToothSeg |
| 官方仓库链接 | [MIC-DKFZ/ToothSeg](https://github.com/MIC-DKFZ/ToothSeg) |
| 基础框架 | nnU-Net v2 |
| 当前优先接入分支 | 语义分割分支 |
| 当前主要目标 | 输入一例牙科 CBCT 图像，输出牙齿语义分割结果 |
| 输入数据类型 | 三维医学影像，推荐使用 NIfTI 格式 |
| 输出数据类型 | NIfTI 格式分割标签图 |
| 输出结果含义 | 每个体素被赋予一个标签编号，表示背景或某一类牙齿 |
| 当前项目使用方式 | 后端封装模型推理流程，Slicer 插件前端通过接口发起分割任务 |

## 2. ToothSeg 两个分支说明

| 分支 | 作用 | 当前项目定位 |
| --- | --- | --- |
| 语义分支 | 判断图像中每个区域属于哪一类牙齿标签 | 当前优先封装和验证的核心模型 |
| 实例分支 | 将牙齿区域进一步拆分为独立牙齿实例，使用 border-core 表示 | 后续高级模式可接入 |


## 3. 语义分支参数

| 参数 | 内容 |
| --- | --- |
| 数据集 ID | `121` |
| 数据集名称 | `Dataset121_ToothFairy2_Teeth` |
| 模型配置 | `3d_fullres_resample_torch_256_bs8_ctnorm` |
| Trainer | `nnUNetTrainer_onlyMirror01_DASegOrd0` |
| Fold | `5` |
| Checkpoint | `checkpoint_final.pth` |
| 网络结构 | 3D PlainConvUNet |
| Patch Size | `256 x 256 x 256` |
| 训练 Batch Size | `8` |
| 目标 spacing | `0.3 x 0.3 x 0.3 mm` |
| 归一化方式 | CTNormalization |
| 标签数量 | 背景 + 32 类牙齿 |
| 权重文件大小 | 约 `237.55 MB` |
| 参数量 | 约 `88.236 M`，即约 8823 万参数 |
| 训练轮次记录 | `1000 epoch` |

## 4. 实例分支参数

| 参数 | 内容 |
| --- | --- |
| 数据集 ID | `123` |
| 数据集名称 | `Dataset123_ToothFairy2fixed_teeth_spacing02_brd3px` |
| 模型配置 | `3d_fullres_resample_torch_192_bs8_ctnorm` |
| Trainer | `nnUNetTrainer` |
| Fold | `5` |
| Checkpoint | `checkpoint_final.pth` |
| 网络结构 | 3D PlainConvUNet |
| Patch Size | `192 x 192 x 192` |
| 训练 Batch Size | `8` |
| 目标 spacing | `0.2 x 0.2 x 0.2 mm` |
| 归一化方式 | CTNormalization |
| 标签数量 | 背景 + center + border |
| 权重文件大小 | 约 `235.48 MB` |
| 参数量 | 约 `88.212 M`，即约 8821 万参数 |
| 训练轮次记录 | `1000 epoch` |




## 5. 硬件运行说明

| 来源 | 说明 |
| --- | --- |
| ToothSeg 官方 README | 未给出明确的最低 RAM 或最低 VRAM 配置表 |
| ToothSeg 官方训练命令 | ToothFairy2 语义分支训练使用 `4` 张 GPU |
| ToothSeg 官方训练命令 | ToothFairy2 实例分支训练使用 `4` 张 GPU |
| ToothSeg 官方训练命令 | In-house 语义分支训练使用 `8` 张 GPU |
| ToothSeg 官方训练命令 | In-house 实例分支训练使用 `4` 张 GPU |
| 推理阶段官方提示 | README 中提到需要降低存储占用和 RAM 占用 |
| 当前项目判断 | 原版 ToothSeg 并不轻量，本地部署需要降采样、低内存推理和任务恢复机制 |


## 6. 当前项目推荐运行策略

| 场景 | 推荐策略 |
| --- | --- |
| 本地低配测试 | 只运行语义分支，并提高 `spacing_mm` |
| 本地正式试用 | 语义分支 + 可复用中间结果 + 低内存推理 |
| 云端测试 | 使用 GPU 实例运行语义分支，确认输出文件结构和标签效果 |
| 高级模式 | 再接入实例分支和 ToothSeg 完整后处理流程 |
| 后续优化 | 考虑模型蒸馏、轻量化、裁剪 ROI、分块推理 |

 |
