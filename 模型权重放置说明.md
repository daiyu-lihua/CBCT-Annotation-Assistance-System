# 模型权重放置说明

权重文件因体积原因不随仓库上传，本文档说明它们的当前位置、运行时放回的方法，以及目录结构。代码对权重的寻址固定为 `implementation/model/weights/`，放回后无需任何配置改动。

## 权重现状对照

| 项目 | 位置 |
| --- | --- |
| 仓库内规范位置（代码寻址此处） | `implementation/model/weights/` |
| 当前实际位置（2026-09-02 起移出仓库） | 仓库同级 `../model_weights/`，即本仓库上一级目录下 |
| 输出结果（一并移出） | 仓库同级 `../CBCT_data/outputs/` |

移出的原因很直接：四个 checkpoint 单文件均为 235.5 至 237.5 MB，超过 GitHub 对单文件 100 MB 的硬性限制，强行推送会被服务端拒绝。权重与运行产物依赖 `.gitignore` 排除，因此它们缺席不影响仓库其他部分的提交与推送。

## 运行时如何放回

服务端 `toothseg_server.py` 与推理入口 `run_toothseg.py` 均按固定路径寻找权重，把 `model_weights/` 整个目录拷回（或剪切回）仓库内 `implementation/model/weights/` 即可恢复推理能力：

```
CBCT-Annotation-Assistance-System/
  implementation/model/weights/          ← 拷回目标
    Dataset121_ToothFairy2_Teeth/        ← 语义分支（33 类牙齿区域）
      nnUNetTrainer_onlyMirror01_DASegOrd0__nnUNetPlans__3d_fullres_resample_torch_256_bs8_ctnorm/
        fold_5/checkpoint_final.pth      ← 推理实际加载（237.5 MB）
        fold_5/checkpoint_best.pth
        plans.json  dataset.json  dataset_fingerprint.json
    Dataset123_ToothFairy2fixed_teeth_spacing02_brd3px/   ← 实例分支（border-core，0.2mm）
      nnUNetTrainer__nnUNetPlans__3d_fullres_resample_torch_192_bs8_ctnorm/
        fold_5/checkpoint_final.pth      ← 推理实际加载（235.5 MB）
        fold_5/checkpoint_best.pth
        plans.json  dataset.json  dataset_fingerprint.json
```

放回后启动 `user/launcher/一键启动ToothSeg服务.bat`，通过 GET /status 可确认两个 checkpoint 均被识别。若只是想验证插件与服务联通而不跑真实推理，`mock_server.py` 不依赖权重，随时可用。

`.gitignore` 已包含 `implementation/model/weights/` 与 `implementation/model/work/`（后者是 nnU-Net 预处理缓存目录，首次推理时生成），因此权重放回原位后执行 `git add .` 也不会把它们带入版本库。

## 其他放置方式

若不便把权重放回仓库内，也可以保持权重在外部位置并通过环境变量指定。`run_toothseg.py` 支持 `TOOTHSEG_NNUNET_RESULTS` 变量指向权重根目录（即包含 Dataset121、Dataset123 两个子目录的那一层），服务端调用推理子进程时也会透传该变量：

```bat
set TOOTHSEG_NNUNET_RESULTS=E:\trae_file\大创\model_weights
```

日常使用推荐直接放回规范位置，少一个环境变量依赖，换机器时也更容易排查。

## 权重来源与输出目录

两组权重来自 ToothSeg 交接包（ToothSegWork 的 nnUNet_results），训练配置与交接文档一致，本次接入未做再训练。历史推理产物（jobs、export、agent_log.jsonl）现保存在仓库同级 `CBCT_data/outputs/`；服务端下次运行会在仓库内重建 `data/outputs/toothseg/`，新旧产物互不影响，`.gitignore` 对两者均生效。
