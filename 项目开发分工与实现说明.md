# 牙科 CBCT 辅助标注系统开发协作说明

> 适用对象：本项目四名团队成员。  
> 编写目的：帮助没有完整项目开发经验的成员理解“这个项目最终要做成什么、整体怎么搭、每个人负责什么、如何用 GitHub 协作推进”。

## 0. 先读结论

本项目最终要做的不是单独一个 AI 模型，也不是单独一个标注软件，而是一套围绕牙科 CBCT 数据标注的本地化人机协同系统。

一句话概括：

```text
用 AI 先给牙科 CBCT 做初步分割，
再让人在 3D Slicer 里修正，
再由 Agent 检查标签问题，
最后导出可用于训练的数据，
并把修正后的数据反过来提升模型。
```

团队开发时建议分成两条主线并行推进：

```text
模型与数据组：负责数据、预处理、模型训练、半监督优化。
系统与 Agent 组：负责 3D Slicer 插件、本地推理服务、Agent、标签质检。
```

这两条线可以同时开发。系统组前期可以先用假模型或已有模型调试流程；模型组也可以独立训练自己的分割模型。等双方都有初步成果后，再通过统一接口集成。

## 1. 项目成熟体定位

### 1.1 成熟体是什么

项目成熟体是一套：

```text
牙科 CBCT 实例级辅助标注系统
```

它主要用于高校实验室、医院院内科研标注、口腔 AI 数据集建设等场景。

成熟系统应该包含四个核心部分：

```text
3D Slicer 插件
  + 本地 AI 推理服务
  + 标注流程 Agent
  + 数据质检与导出规范
```

### 1.2 成熟体不是什么

为了避免方向跑偏，需要先明确三点：

- 它不是临床诊断系统，不负责判断疾病。
- 它不是完全自动标注系统，人工修正仍然是核心环节。
- 它不是只训练一个模型，模型只是系统中的一个模块。

### 1.3 项目的真正价值

当前牙科 CBCT 标注存在几个问题：

- CBCT 是三维数据，切片多，人工逐层勾画很慢。
- 单颗牙、牙根、牙髓、种植体边界复杂。
- 不同标注人员容易出现编号混乱。
- 医学影像数据涉及隐私，不适合随便上传云端。
- 训练口腔 AI 模型需要大量高质量标签。

本项目要解决的是：

```text
提高标注效率
降低操作门槛
保证标签规范
减少数据外传风险
形成可持续积累的数据闭环
```

## 2. 核心术语说明

### 2.1 CBCT

CBCT 是锥形束 CT，牙科中常用来拍摄口腔和颌面部三维影像。它不是普通二维图片，而是一组连续切片组成的三维体数据。

### 2.2 实例级分割

分割是指把图像中的目标区域标出来。

实例级分割不仅要知道“哪里是牙齿”，还要区分“这是第 11 号牙、这是第 12 号牙、这是第 21 号牙”。

简单理解：

```text
普通分割：这片区域是牙齿。
实例级分割：这片区域是某一颗具体编号的牙齿。
```

### 2.3 ROI

ROI 是感兴趣区域。比如一整份 CBCT 数据很大，但模型只需要处理牙弓区域或某颗牙附近区域，这个被选中的区域就是 ROI。

### 2.4 LabelMap 和 Segmentation

LabelMap 是一种标签图，每个像素或体素用数字表示类别或编号。

Segmentation 是 3D Slicer 中常用的分割结果表示方式，更适合人工查看和修正。

项目中常见流程是：

```text
AI 输出 mask
  -> 加载到 3D Slicer 中成为 Segmentation
  -> 人工修正
  -> 导出 LabelMap 用于模型训练
```

### 2.5 半监督学习

半监督学习是指用少量已标注数据，加上大量未标注数据一起训练模型。

本项目适合半监督学习，因为 CBCT 精细标注很费时间，短期内很难获得大量高质量人工标签。

### 2.6 伪标签

伪标签是模型给未标注数据预测出的标签。它不一定完全正确，所以需要筛选和人工抽查。

项目中的半监督闭环可以理解为：

```text
少量人工标注数据训练初始模型
  -> 模型预测未标注数据
  -> 得到伪标签
  -> 质量筛选和人工修正
  -> 加入训练集继续训练
```

### 2.7 Agent

本项目中的 Agent 可以先理解成“流程助手”。

它不直接做医学诊断，也不应该完全自主修改标签。它主要负责：

- 记录当前标注状态；
- 提醒下一步该做什么；
- 推荐推理模式；
- 调用模型推理服务；
- 调用标签检查脚本；
- 记录历史问题；
- 把高频问题沉淀成经验规则。

### 2.8 端侧部署和本地部署

本项目中“端侧部署”可以理解为：模型推理尽量在医院或实验室本地设备上运行，而不是把原始 CBCT 上传到公有云。

更准确地说：

```text
本地部署：在本机、实验室服务器、医院内网服务器运行。
端侧部署：在靠近使用现场的设备上运行，例如工作站、边缘计算盒、院内服务器。
云端部署：把数据传到外部云服务器，由云服务器运行模型。
```

本项目可以训练阶段借助实验室 GPU 或合规云端算力，但成熟系统的推理和标注流程应尽量强调本地闭环。

## 3. 整体架构

### 3.1 总体架构图

项目建议采用五层架构：

```text
用户
  |
  v
3D Slicer 可视化标注前端
  |
  v
标注流程 Agent
  |
  v
本地 AI 推理服务端
  |
  v
模型算法层 + 数据管理层
```

### 3.2 各层负责什么

#### 第一层：用户

用户负责操作系统完成标注任务。用户可以是团队成员、实验室标注人员或医院科研人员。

用户主要做：

- 打开 CBCT；
- 选择 ROI；
- 查看 AI 分割结果；
- 手动修正错误；
- 确认质检报告；
- 导出最终标签。

#### 第二层：3D Slicer 前端

3D Slicer 是医学影像查看和标注工具。

前端负责：

- 显示 CBCT 三维影像；
- 提供按钮和操作界面；
- 选择 ROI；
- 调用后端接口；
- 显示 AI 返回的 mask；
- 支持人工修正；
- 导出 LabelMap 或 Segmentation。

#### 第三层：Agent 调度层

Agent 是流程组织者。

Agent 负责：

- 读取当前病例状态；
- 判断是否已经导入图像、选择 ROI、完成推理、完成质检；
- 根据数据大小和硬件状态推荐推理模式；
- 调用推理服务；
- 调用标签检查；
- 生成提示信息；
- 保存任务日志。

#### 第四层：本地 AI 推理服务端

推理服务端是模型运行的地方。

建议用 FastAPI 实现。它负责：

- 接收 3D Slicer 或 Agent 发来的请求；
- 读取图像；
- 做预处理；
- 调用模型；
- 做后处理；
- 保存预测 mask；
- 返回结果路径或报告。

#### 第五层：模型算法层 + 数据管理层

模型算法层负责训练和推理。

数据管理层负责保存：

- 原始 CBCT；
- 人工标签；
- AI 预测标签；
- 修正后标签；
- 标签规范；
- 质量报告；
- 任务日志；
- 训练数据索引。

## 4. 系统运行逻辑

成熟系统的一次完整使用流程如下：

```text
1. 用户打开 3D Slicer。
2. 用户导入 CBCT 数据。
3. 系统加载牙位标签模板。
4. 用户选择牙弓 ROI 或单牙 ROI。
5. 3D Slicer 将图像路径和 ROI 信息交给 Agent。
6. Agent 检查当前状态和硬件状态。
7. Agent 推荐快速、均衡或精细推理模式。
8. Agent 调用本地 AI 推理服务。
9. 推理服务读取图像并完成预处理。
10. 模型生成初步分割 mask。
11. 推理服务完成后处理并保存结果。
12. 3D Slicer 加载 AI 结果。
13. 用户人工修正边界、漏标、错标和编号。
14. Agent 调用标签质量检查脚本。
15. 系统生成 quality_report.json 和 quality_report.md。
16. 用户根据报告复核问题。
17. 系统导出训练数据。
18. 任务日志进入经验库。
19. 修正后的标签可用于下一轮模型训练。
```

这条流程是整个项目开发的主线。每个模块都应该服务于这条流程。

## 5. 实现路径

### 5.1 第一阶段：共同确定接口契约

在正式分头开发前，四个人必须先一起确定接口契约。

接口契约就是提前约定：

```text
系统各模块之间怎么传数据，
输入是什么，
输出是什么，
文件放在哪里，
命名规则是什么。
```

如果没有接口契约，两个组即使都写出了代码，最后也可能接不上。

建议先确定这些内容：

```text
输入图像格式：DICOM、nii.gz、nrrd
ROI 表示方式：起点坐标 + 尺寸，或 3D Slicer ROI 节点
模型输入格式：[1, 1, D, H, W]
模型输出格式：nrrd、nii.gz 或 numpy mask
标签编号规则：天然牙、牙髓、种植体分别如何编号
推理接口：POST /predict
检查接口：POST /check_label
状态接口：GET /status
配置接口：GET /config
质量报告：quality_report.json 和 quality_report.md
```

### 5.2 第二阶段：系统组先用假模型调通流程

系统组不需要等模型组训练完成。

前期可以使用：

- 假 mask；
- 简单阈值分割；
- 手工准备的 LabelMap；
- 已有开源模型输出；

来调通以下流程：

```text
3D Slicer 选择 ROI
  -> 调用本地服务
  -> 返回一个 mask
  -> Slicer 加载显示
  -> 人工修正
  -> 导出
```

这一阶段的目标是证明系统框架可行。

### 5.3 第三阶段：模型组跑通基础模型

模型组前期不追求复杂模型，先跑通基础模型。

推荐路线：

```text
数据读取
  -> 预处理
  -> 训练 3D U-Net 或 nnU-Net
  -> 得到初步预测 mask
  -> 计算 Dice 和 IoU
```

只要模型能正常训练、正常推理、正常输出 mask，就可以先交给系统组集成。

### 5.4 第四阶段：第一次集成

当系统组有可调用接口，模型组有可用权重后，开始第一次集成。

目标是跑通完整链路：

```text
真实 CBCT
  -> 真实 ROI
  -> 真实模型预测
  -> Slicer 显示
  -> 人工修正
  -> 标签检查
  -> 导出训练数据
```

第一次集成时不要追求完美，重点是发现接口、格式、路径、依赖环境的问题。

### 5.5 第五阶段：半监督与 Agent 增强

完整闭环跑通后，再做增强。

模型组增强：

- 生成伪标签；
- 筛选伪标签；
- 加入人工修正数据；
- 迭代训练；
- 比较模型效果变化。

系统组增强：

- 完善 Agent 状态记录；
- 加入推理模式推荐；
- 完善标签质检；
- 生成可读报告；
- 沉淀经验规则。

### 5.6 第六阶段：端侧部署和成果整理

最后再考虑：

- ONNX 导出；
- TensorRT 加速；
- 国产化算力平台适配；
- 系统截图；
- 技术文档；
- 软著材料；
- 研究报告。

## 6. 四人分工策略

### 6.1 总体分组

建议分为两组：

```text
A 组：模型与数据组，2 人。
B 组：系统与 Agent 组，2 人。
```

这样分工的理由：

- 模型训练和系统开发可以并行。
- 两组之间只需要通过接口契约连接。
- 每组内部两个人可以互相检查、互相补位。
- 每个人都有明确职责，避免多人同时挤在同一段代码上。

### 6.2 A 组：模型与数据组

#### 角色 A1：数据与评估负责人

主要负责：

- 整理 CBCT 数据；
- 建立病例目录结构；
- 制定标签模板；
- 制定牙位编号规则；
- 处理 DICOM、nii.gz、nrrd 等格式；
- 编写预处理脚本；
- 划分训练集、验证集、测试集；
- 统计 Dice、IoU、实例识别准确率；
- 参与人工标注和复核。

阶段性输出：

```text
数据目录规范
标签编号规范
预处理脚本
数据集划分文件
评价指标脚本
标注质量统计表
```

#### 角色 A2：模型训练负责人

主要负责：

- 搭建或复现 3D U-Net、nnU-Net 或 MONAI 模型；
- 跑通训练流程；
- 跑通推理流程；
- 生成 AI 初分割 mask；
- 设计半监督伪标签流程；
- 尝试 2.5D 或轻量化模型；
- 导出 PyTorch 权重和 ONNX 模型；
- 整理模型效果对比。

阶段性输出：

```text
训练代码
推理代码
基础模型权重
预测结果样例
半监督训练脚本
ONNX 模型
模型效果对比表
```

### 6.3 B 组：系统与 Agent 组

#### 角色 B1：3D Slicer 前端负责人

主要负责：

- 学习 3D Slicer 插件开发基础；
- 搭建插件界面；
- 实现图像导入入口；
- 实现标签模板加载入口；
- 实现 ROI 选择与读取；
- 调用后端推理接口；
- 将返回 mask 加载到 Slicer；
- 对接人工修正流程；
- 实现导出按钮；
- 整理系统演示截图。

阶段性输出：

```text
3D Slicer 插件原型
ROI 选择功能
后端接口调用功能
AI 结果显示功能
导出入口
演示截图
```

#### 角色 B2：后端与 Agent 负责人

主要负责：

- 搭建 FastAPI 本地服务；
- 定义 `/predict`、`/status`、`/config`、`/check_label` 接口；
- 接入模型推理脚本；
- 编写标签质量检查脚本；
- 维护 Agent 状态文件；
- 实现流程提示；
- 实现推理模式推荐；
- 保存任务日志；
- 生成质量报告。

阶段性输出：

```text
FastAPI 服务端
推理接口
状态接口
配置接口
label_checker.py
quality_report.json
quality_report.md
Agent 状态文件
任务日志
```

## 7. GitHub 协作说明

### 7.1 GitHub 是什么

GitHub 是团队共享代码和管理项目进度的平台。

可以把它理解为：

```text
代码仓库 + 任务看板 + 修改记录 + 队友代码审查工具
```

团队使用 GitHub 的目标不是为了“显得专业”，而是为了：

- 防止代码丢失；
- 让每个人知道别人做了什么；
- 避免多人互相覆盖代码；
- 保留每次修改记录；
- 方便阶段性集成和回退。

### 7.2 推荐仓库结构

建议仓库名称：

```text
cbct-annotation-system
```

推荐目录：

```text
cbct-annotation-system/
  README.md
  docs/
    project_plan.md
    api_contract.md
    label_rules.md
    development_workflow.md
  configs/
    labels.yaml
    inference_modes.yaml
    model_config.yaml
  data_tools/
    convert_dicom.py
    preprocess.py
    split_dataset.py
  training/
    train.py
    infer.py
    semi_supervised.py
    export_onnx.py
  inference_server/
    main.py
    routes/
    services/
    schemas/
  slicer_extension/
    CBCTAnnotator/
  agent/
    state_manager.py
    planner.py
    label_checker.py
    memory/
    skills/
  tests/
    test_preprocess.py
    test_api.py
    test_label_checker.py
  demo_cases/
    README.md
```

### 7.3 推荐分支

分支可以理解为“每个人自己的开发空间”。

建议设置：

```text
main                  稳定版本，只放可以展示或阶段验收的代码
dev                   日常集成版本
feature/data          数据处理开发
feature/model         模型训练开发
feature/slicer        3D Slicer 插件开发
feature/backend-agent 后端与 Agent 开发
```

规则：

- 不直接把代码提交到 `main`。
- 日常开发先提交到自己的 `feature` 分支。
- 功能完成后合并到 `dev`。
- `dev` 测试稳定后再合并到 `main`。

### 7.4 Pull Request 是什么

Pull Request 简称 PR，可以理解为：

```text
我写完了一个功能，请队友检查一下，确认没问题后合并。
```

每个 PR 应该写清楚：

- 做了什么；
- 改了哪些文件；
- 如何测试；
- 是否影响其他模块；
- 是否需要别人配合。

### 7.5 Issue 是什么

Issue 可以理解为任务卡片。

每个任务建议开一个 Issue，例如：

```text
实现 DICOM 转 nii.gz 脚本
编写 /predict 接口
完成 3D Slicer ROI 读取
实现标签连通域检查
跑通 nnU-Net 基础训练
```

每个 Issue 应写清楚：

```text
任务目标
负责人
预计完成时间
输入文件
输出结果
完成标准
关联分支
```

## 8. 初学团队推荐工作流

### 8.1 每周节奏

建议每周固定一次小组同步。

每次同步只讨论四件事：

```text
1. 上周完成了什么。
2. 本周要完成什么。
3. 当前卡在哪里。
4. 是否影响其他人的接口或文件格式。
```

### 8.2 每个人每天怎么推进

每天开发前：

```text
1. 拉取最新 dev 分支。
2. 切到自己的 feature 分支。
3. 查看自己负责的 Issue。
4. 小步修改代码。
5. 本地运行测试或脚本。
6. 提交 commit。
7. 推送到 GitHub。
```

### 8.3 Commit 怎么写

Commit 是一次代码修改记录。

建议格式：

```text
模块: 做了什么
```

例子：

```text
data: add dicom to nifti conversion script
model: add basic monai unet training config
backend: add predict api skeleton
slicer: add roi selection button
agent: add label volume check
docs: update api contract
```

### 8.4 合并前检查清单

提交 PR 前先检查：

```text
代码能否运行
路径是否写死
是否误传了隐私数据
是否更新了文档
是否影响接口格式
是否有示例输入和输出
```

特别注意：不要把真实患者原始 CBCT 数据上传到公开 GitHub 仓库。

## 9. 接口契约示例

### 9.1 `/predict` 请求示例

```json
{
  "case_id": "case_0001",
  "image_path": "data/case_0001/raw/image.nii.gz",
  "roi": {
    "start": [40, 60, 20],
    "size": [128, 128, 96]
  },
  "mode": "balanced",
  "output_dir": "data/case_0001/predictions"
}
```

### 9.2 `/predict` 返回示例

```json
{
  "case_id": "case_0001",
  "status": "success",
  "mask_path": "data/case_0001/predictions/ai_pred.nii.gz",
  "message": "AI initial segmentation finished."
}
```

### 9.3 `/check_label` 请求示例

```json
{
  "case_id": "case_0001",
  "label_path": "data/case_0001/labels/corrected_label.nii.gz",
  "label_rule_path": "configs/labels.yaml"
}
```

### 9.4 `/check_label` 返回示例

```json
{
  "case_id": "case_0001",
  "status": "warning",
  "report_path": "data/case_0001/reports/quality_report.json",
  "issues": [
    {
      "type": "multi_component",
      "label": 112,
      "message": "标签 112 存在多个不连续连通区域，建议复核牙根部分。"
    }
  ]
}
```

## 10. 相关工具说明

### 10.1 3D Slicer

医学影像查看和标注工具。项目中负责显示 CBCT、选择 ROI、显示 AI 分割结果、人工修正和导出标签。

### 10.2 PyTorch

深度学习框架。项目中用于训练牙齿分割模型。

### 10.3 MONAI

医学 AI 工具库，基于 PyTorch。项目中可用于医学图像预处理、数据增强、模型搭建和训练流程。

### 10.4 nnU-Net

医学图像分割常用框架。适合快速建立基础分割模型，用来作为项目早期模型基线。

### 10.5 SimpleITK

医学图像处理库。项目中用于读取 DICOM、nii.gz、nrrd，并进行重采样、方向校正、灰度处理。

### 10.6 FastAPI

Python 后端接口框架。项目中用于搭建本地推理服务，让 3D Slicer 能通过接口调用模型。

### 10.7 ONNX

模型交换格式。PyTorch 模型导出为 ONNX 后，更方便部署到不同推理平台。

### 10.8 TensorRT

NVIDIA 的模型推理加速工具。适合在 NVIDIA GPU 或 Jetson 设备上提升推理速度。

### 10.9 GitHub

代码托管和团队协作平台。项目中用于保存代码、管理任务、发起 PR、记录版本。

### 10.10 Markdown

轻量文档格式。本文件就是 Markdown。项目中的接口说明、标签规范、开发记录、会议纪要都建议用 Markdown 编写。

## 11. 开发优先级

项目开发要避免一开始做得太大。建议按优先级推进：

```text
第一优先级：完整闭环
CBCT 导入 -> ROI 选择 -> AI 初分割 -> Slicer 显示 -> 人工修正 -> 导出

第二优先级：标签质检
空标签检查、编号检查、连通域检查、体积异常检查、导出格式检查

第三优先级：模型增强
基础分割模型、实例分割、半监督伪标签训练

第四优先级：Agent 增强
状态感知、流程提示、推理模式推荐、经验沉淀

第五优先级：部署优化
ONNX 导出、TensorRT 加速、国产化算力平台适配
```

## 12. 最小可展示 Demo 标准

如果项目中期需要展示，最小 Demo 不要求模型非常准确，但必须能展示完整流程。

最低标准：

```text
1. 能打开一例脱敏 CBCT。
2. 能选择 ROI。
3. 能调用后端服务。
4. 能返回一个 AI 初分割 mask。
5. 能在 3D Slicer 中显示 mask。
6. 能人工修正。
7. 能导出标签文件。
8. 能生成标签质量检查报告。
```

达到这个标准后，项目就已经具备系统雏形。

## 13. 常见风险与应对

### 13.1 模型训练效果不好

应对：

- 先保证流程跑通；
- 用简单模型做基线；
- 增加人工修正；
- 用半监督伪标签逐步扩充数据；
- 不把项目成败完全压在模型精度上。

### 13.2 系统和模型接不上

应对：

- 第一周先写接口契约；
- 固定输入输出格式；
- 系统组先用假 mask 调通；
- 模型组按约定格式输出结果。

### 13.3 GitHub 协作混乱

应对：

- 每个人只在自己的 feature 分支开发；
- 每个功能对应一个 Issue；
- 每次合并都走 PR；
- 重要接口变更必须更新文档。

### 13.4 数据隐私问题

应对：

- 不上传真实患者原始数据到公开仓库；
- Demo 数据必须脱敏；
- GitHub 仓库中只放代码、配置模板和说明文档；
- 大文件和敏感数据使用本地或内网存储。

## 14. 总结

本项目最适合采用：

```text
两条主线并行开发
  + 统一接口契约
  + 阶段性集成
  + 小步快跑
```

模型与数据组负责让 AI 分割能力逐步变强；系统与 Agent 组负责让系统流程真正可用。前期的关键不是把每个模块做到最好，而是尽快跑通完整闭环。

只要完成：

```text
CBCT 导入
  -> AI 初分割
  -> 人工修正
  -> 标签检查
  -> 结果导出
```

项目就具备了清晰的成熟体雏形。后续的半监督学习、Agent 自进化、端侧部署和国产化平台适配，都可以在这个闭环上逐步增强。
